{
  "name": "my-portfolio-bootstrap",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "cross-env NODE_ENV=production next build",
    "export": "next export",
    "deploy": "if exist docs rd /s /q docs && if exist .next rd /s /q .next && cross-env NODE_ENV=production next build && (if not exist docs\\.nojekyll type NUL > docs\\.nojekyll) && git add -f docs && git commit -m \"deploy site\" && git push"
  },
  "dependencies": {
    "bootstrap": "^5.3.7",
    "next": "15.4.6",
    "next-themes": "^0.4.6",
    "react": "19.1.0",
    "react-bootstrap": "^2.10.10",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "cross-env": "^10.0.0",
    "eslint": "^9",
    "eslint-config-next": "15.4.6"
  }
}
